init:
  # Fix line endings on Windows
  - git config --global core.autocrlf true
environment:
  matrix:
    - nodejs_version: "8.9.4"
    - nodejs_version: "10.0.1"
platform:
  - x86
  - x64
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
  - npm test
test_script:
  - node --version && npm --version
build: off
matrix:
  fast_finish: true
cache:
  - node_modules -> package.json
